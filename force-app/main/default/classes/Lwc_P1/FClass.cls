public with sharing class FClass {
    public static void  concatMethod(String name , String lastName) {
String a= name.toUpperCase();
String b= lastName.toUpperCase();

system.debug(a +' '+ b);
    }
}